
import { DriverHubLayout } from '@/components/app/driver-hub-sidebar';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <DriverHubLayout>{children}</DriverHubLayout>;
}
